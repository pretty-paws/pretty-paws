import styled from 'styled-components';

export const StyledSocialNetsAuthBox = styled.div`
  & .auth-text {
    margin-top: 32px;
    color: #111;
    font-size: 18px;
    font-weight: 400;
  }

  & .auth-icons-box {
    margin: 25px 0 36px;
    display: flex;
    gap: 32px;
  }

  & .auth-icons {
    cursor: pointer;
  }
`;
